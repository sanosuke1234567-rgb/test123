# 每日食谱营养记录

一个简单的 Flask 应用，用于记录每日食谱并自动计算碳水/蛋白/脂肪与卡路里摄入。

## 功能
- 记录食谱名称、日期与人数。
- 选择食材、输入重量，实时预览宏量营养与卡路里。
- 自动计算单人份摄入与当天总营养。
- 内置常见食材数据库，可直接调用。

## 启动

```bash
python app.py
```

浏览器访问 `http://localhost:5000`。

如需安装依赖：

```bash
pip install -r requirements.txt
```
